[Unit]
Description=MSL shell listener (vsock)
After=network.target

[Service]
ExecStart=/bin/sh -c "modprobe vmw_vsock_virtio_transport 2>/dev/null; exec /usr/local/bin/shellinit"
Restart=always

[Install]
WantedBy=multi-user.target
