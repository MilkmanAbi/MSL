[Unit]
Description=MSL network-visibility listener (vsock)
After=network.target

[Service]
ExecStart=/bin/sh -c "modprobe vmw_vsock_virtio_transport 2>/dev/null; exec /usr/local/bin/trafficd"
Restart=always

[Install]
WantedBy=multi-user.target
